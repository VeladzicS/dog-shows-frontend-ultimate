# Http-Client

1. Iz `.config` foldera iskopirati datoteku `http-client.private.env.json` u `.http` folder te podesiti vlastite podatke za servere
2. Excludati `.http` iz sinkronizacije na servere
3. Svaka grupa requestova treba biti u svojoj `.http` datoteci
